#include "socket.h"

int main(int argc, char **argv) 
{
    printf("== Build a web client, socket ==\n\n");
    
    int sockfd, n;
    int sendbytes;
    struct sockaddr_in servaddr;
    char sendline[MAXLINE];
    char recvline[MAXLINE];

    return 0;
}